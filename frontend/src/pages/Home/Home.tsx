import { Box, Button } from '@chakra-ui/react';

import './Home.scss';

const Home = () => {
    return (
        <Box>
            <Button variant={'link'}>click me</Button>
        </Box>
    );
};

export default Home;
