import { Box } from '@chakra-ui/react';
import MonthView from './MonthView';
const CalendarView = () => {
    return (
        <Box>
            <MonthView />
        </Box>
    );
};

export default CalendarView;
